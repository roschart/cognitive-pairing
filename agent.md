# Agent Instructions

Before doing anything else in this project, execute the
`cp-hydrate` skill. Read `.cp/canon.md`, the latest checkpoint
in `.cp/checkpoints/`, `.cp/memory/active.md`, and any active
`plan-*.md` files. Then display a short alignment summary so
the human can confirm you loaded the right context.

If `.cp/` does not exist, offer to create the initial
structure.
